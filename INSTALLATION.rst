============
Installation
============

At the command line::

    $ pip install oktauilib

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv oktauilib
    $ pip install oktauilib

Or, if you are using pipenv::

    $ pipenv install oktauilib

Or, if you are using pipx::

    $ pipx install oktauilib
